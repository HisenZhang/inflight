# Cloudflare Pages configuration for IN-FLIGHT
# Static PWA deployment using Wrangler CLI

name = "inflight"
compatibility_date = "2025-11-01"

# Main app: Deploy static files from repository root
# This is a Workers Assets project (static files only)
[assets]
directory = "."
