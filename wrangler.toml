# Cloudflare Pages configuration
# This file is primarily for documentation deployment via VitePress
# The main app is deployed as a static site without build step

name = "inflight"
compatibility_date = "2025-11-01"

# Custom domain
# Main app domain: in-flight.org
# Docs domain: docs.in-flight.org (or separate Pages project)

# No [site] configuration needed - main app is static HTML/CSS/JS
# Docs deployment should use: npm run docs:build && npx wrangler pages deploy docs/.vitepress/dist
